spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/security_example_db?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    com.ll.security: debug
